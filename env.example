# Frontend Configuration
# API endpoint (relative path for Next.js API proxy - don't change this)
NEXT_PUBLIC_API_URL=/api

# Default assistant ID (change to match your LangGraph backend)
NEXT_PUBLIC_ASSISTANT_ID=agent

# Backend Configuration
# LangGraph backend server URL (change port if your backend runs on different port)
LANGGRAPH_API_URL=http://localhost:3000

# Optional: LangSmith API key for tracing (keep private, not exposed to client)
LANGSMITH_API_KEY=

# Production Configuration (uncomment and modify for production deployment)
# NEXT_PUBLIC_ASSISTANT_ID="your-assistant-id"
# LANGGRAPH_API_URL="https://your-production-langgraph-server.com"
# NEXT_PUBLIC_API_URL="https://your-frontend-domain.com/api"
# LANGSMITH_API_KEY="lsv2_..."
